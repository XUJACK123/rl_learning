## PPO算法
1. critic网络
- Critic 的核心工作就是在计算状态价值，可以理解为就是利用神经网络来加速近似求解贝尔曼期望方程的拟合器
- 和贝尔曼方程的关系：
    - 工具与法则的关系：Critic 依靠贝尔曼方程来学习，Critic会试图一直让自己的预测尽量靠近基于贝尔曼期望方程算出来的目标值，以此不断修正自己的估计准确度
    - 为什么不能直接用贝尔曼方程：状态的数量太大了
    - 为什么不用类似于蒙特卡洛的方法来进行计分：蒙特卡洛会导致方差极大以及效率较低
    - 怎么知道什么时候算模拟够格：在 PPO 中，Critic 不需要做到 100% 精确，它只需要保证“相对大小判断正确”，PPO 关心的是相对好坏
    - 为什么不在一开始就直接算好critic方程：Critic 拟合的是当前这个特定 Actor 在该状态下的期望回报，Critic 的学习率通常会设得比 Actor 稍大以保证Critic 能够“跟上进度”
- 如何微调更新critic方程
    - 计算估算误差（TD Error）与 Loss：Critic 算出自己当前对状态和预测值，将其与贝尔曼目标对比并构造均方误差损失
    - 利用反向传播，向着让预测值更接近贝尔曼目标的方向，微调一小步神经网络参数
    - 不需要计算出“最终的真实值”，而是使用时序差分的方法来对于critic来调整

## 当前进度
- PPO算法还没写完，未完待续
- walker的环境以及搭好了